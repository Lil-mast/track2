/**
 * Placeholder for AWS Aurora PostgreSQL data repository.
 *
 * Implementation steps:
 * 1. Install `pg` or `@vercel/postgres` / Drizzle ORM / Prisma
 * 2. Define schema matching src/types/*
 * 3. Implement IDataRepository methods with SQL queries
 * 4. Add connection pooling via RDS Proxy
 * 5. Swap in getDataRepository() when AURORA_ENABLED=true
 */
export class AuroraDataRepository {
  // TODO: Implement when Aurora is connected
}
